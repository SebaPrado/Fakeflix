{
    "build": {
      "env": {
        "NODE_ENV": "production"
      },
      "commands": {
        "install": "yarn install",
        "prebuild": "chmod +x node_modules/.bin/vite",
        "build": "yarn build"
      }
    }
  }
  